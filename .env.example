# CMT — Context Migration Tool
# Users provide their own Cerebras API key via the UI.
# This file is for local development reference only.
# Never commit a real API key.

VITE_APP_NAME=Context Migration Tool
